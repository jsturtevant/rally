# Mal Orchestration: Config Format Update (YAML)

**Timestamp:** 2026-02-21T22:47:19Z  
**Agent:** Mal (Lead)  
**Task:** Switch config files from JSON to YAML in PRD  
**Status:** Completed

## What Happened

Mal received directive from user: config files should use YAML, not JSON.

### Work Done

- Updated `docs/PRD.md` §4.3 (File-based State) to reflect:
  - `~/.rally/config.yaml` (was config.json)
  - `~/.rally/projects.yaml` (was projects.json)
  - `~/.rally/active.yaml` (was active.json)
- Updated §4.3 description of `config.js` module to note: "parses/serializes YAML" and flagged as needing hand-rolled YAML parser (zero-dependency constraint)
- Updated all code block examples throughout PRD to show YAML syntax instead of JSON

### Outcome

✓ All config file references now use `.yaml` extension and YAML syntax in docs/PRD.md  
✓ Decision filed in `.squad/decisions/inbox/mal-yaml-config.md`

### Key Decision

**Hand-rolled YAML parser** — Node.js has no built-in YAML support. Since our config structures are flat/shallow (simple key-value + one-level object arrays), a minimal hand-rolled parser covering only what we use is feasible and keeps us zero-dependency.

## Impact

- All team agents must read updated PRD before implementing file I/O
- `config.js` module implementation team (Kaylee or similar) must account for YAML parsing
