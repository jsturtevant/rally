# Orchestration Log: Jayne (Tester)

**Timestamp:** 2026-02-22T001900Z  
**Agent:** Jayne (Tester)  
**Mode:** background  
**Task:** PRD testability & edge case review  

## Outcome

🔴 **5 critical blockers found** (open questions)  
🟡 **12 error-handling gaps identified**  
🟡 **20+ edge case gaps identified**  
⚠️ **Test framework not specified**

### Critical Blockers

1. **YAML parser** — Zero-dependency contradiction. PRD lists js-yaml but decisions.md says hand-rolled parser.
2. **Windows symlink fallback** — §9.7 open. No strategy for Windows without Developer Mode.
3. **Squad invocation mechanism** — §9.1 open. Three options not decided (instructions vs CLI vs VS Code).
4. **Dispatch status lifecycle** — Rules for status transitions (planning → implementing → reviewing → done → cleaned) not defined.
5. **dispatch-context.md format** — Schema and format undefined. §9.4 lists as open.

### Error-Handling Gaps

Missing error specs for:
- Setup command (npx, git, home dir, idempotency)
- Onboard command (clone failures, symlink failures, corruption, timeouts)
- Dispatch command (uncommitted changes, branch collisions, auth expiry)
- Dashboard (empty state, stale entries, concurrent access)
- Exit codes (not defined at all in PRD §8)

### Edge Cases Not Covered

- Idempotency (re-onboard, re-setup, re-run behavior)
- Dispatch collisions (same issue twice, branch collision, worktree collision)
- Multiple projects ambiguity
- Config validation (malformed YAML, missing keys, path normalization)
- Concurrent access (race conditions on active.yaml)

### Test Infrastructure

Test framework choice, mocking strategy (git/gh/npx), fixture management, and integration test patterns not specified. No `docs/TESTING.md`.

**Full review:** `.squad/decisions/inbox/jayne-prd-review.md`
