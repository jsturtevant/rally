# Orchestration: Kaylee - Deny-Tool Enforcement (#151)

**Timestamp:** 2026-02-24T005917Z  
**Agent:** Kaylee (Core Dev)  
**Mode:** Background  
**PR:** #156

## Task

Implement deny-tool flags for issue #151 to restrict Copilot to read-only dispatch operations.

## Outcome

**Status:** ✅ Complete

- **PR #156** opened with deny-tool enforcement
- Replaced file-based copilot-instructions.md approach with `--deny-tool` CLI flags
- Read-only policy embedded in prompt via `-p` flag
- All 396 tests pass

## Files Modified

1. `lib/copilot.js` — Added DENY_TOOLS constant and deny-tool flags to launchCopilot()
2. `lib/dispatch-core.js` — Removed writeCopilotInstructions() call
3. `test/copilot.test.js` — Tests for deny-tool flags
4. `test/dispatch-core.test.js` — Updated tests

## Files Removed

1. `lib/copilot-instructions.js` — No longer needed (file writing replaced)
2. `test/copilot-instructions.test.js` — Associated tests

## Approach

- **Primary enforcement:** `--deny-tool` flags block shell commands (git push, gh pr/issue/repo/api) and MCP tools
- **Defense-in-depth:** Read-only policy prepended to prompt, not written to files
- **No file side effects:** Doesn't modify user worktrees
- **Deny takes precedence:** Works with `--allow-all-tools` flag

## Decision Merged

- kaylee-deny-tool-enforcement.md → decisions.md
- Consolidated with earlier mal-readonly-copilot-enforcement.md proposal
